#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
	char str[20];
	printf("enter the string:");
	gets (str);
	printf("the lenght of the string is :%d",strlen(str));
	getch();
}
